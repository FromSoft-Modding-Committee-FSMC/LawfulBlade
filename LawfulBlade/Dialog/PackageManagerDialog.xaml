<Window x:Class="LawfulBlade.Dialog.PackageManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LawfulBlade.Dialog"
        mc:Ignorable="d"
        Title="Lawful Blade - Package Manager" Height="540" Width="960" Background="#FF101010" Icon="{StaticResource lbIcon}" ShowInTaskbar="False">
    <Grid Background="#FF202020">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="0.75*"/>
            <RowDefinition Height="28px"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#FF101010">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.75*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Panel Contains a list of filters for packages... -->
            <StackPanel x:Name="filterList" Grid.Column="0" Background="#FF202020">
                <TextBlock Foreground="#FFC0C0C0" HorizontalAlignment="Left" Margin="0,3,0,7">Package Filters...</TextBlock>
                <StackPanel Orientation="Horizontal" Margin="4,0,0,0">
                    <CheckBox/>
                    <TextBlock Margin="2,0,0,0" Foreground="#FFF0F0F0">Example</TextBlock>
                </StackPanel>
            </StackPanel>
            
            <!-- Panel is used to store our avaliable packages -->
            <StackPanel x:Name="packageList" Grid.Column="1"/>

            <!-- Panel contains information about the packages... -->
            <StackPanel Grid.Column="2" Background="#FF202020">
                
            </StackPanel>
        </Grid>

        <!-- We can use this area to display status to the user?.. -->
        <Border Grid.Row="1" Background="#FF181818" BorderBrush="#FF101010" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Foreground="#FFF0F0F0" Margin="4,0,4,0">hellohello</TextBlock>
            </ScrollViewer>
        </Border>

            <!-- Button Array -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="0,0,4,0" Background="#FF308FA0" VerticalAlignment="Center">
                <TextBlock Margin="4,0,4,0">Done</TextBlock>
            </Button>
        </StackPanel>
    </Grid>
</Window>
